class Cerberus:

    def on_tick(self, state):

        memory.update(state)

        free_action_phase(state)

        if state.cooldown_active:
            return "WAIT"

        return decide(state)